# EIF — real config files (copy from *.example.json and fill in values)
accounts.json
matters/**/dev.json
matters/**/test.json
matters/**/prod.json

# EIF rendered outputs
matters/**/.rendered/

# Terraform
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfplan
.terraform.lock.hcl
