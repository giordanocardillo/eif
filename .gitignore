# EIF — real config files (copy from *.example.json and fill in values)
accounts.json
matters/**/dev.json
matters/**/test.json
matters/**/prod.json

# EIF rendered outputs
matters/**/.rendered/

# EIF local deployment history (use remote backend for team sharing)
matters/**/.history/

# Terraform
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfplan
.terraform.lock.hcl
